var microposts_view = "<%= escape_javascript(render @microposts) %>"
if(microposts_view != ""){
	var page = parseInt($("#page").val())
	$("#page").val(page + 1)
	$("#microposts").append(microposts_view)
}
else{
	$("#page").parent().hide()
}