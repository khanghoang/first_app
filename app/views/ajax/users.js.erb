var users_view = "<%= escape_javascript(render @users) %>"
if(users_view != ""){
	var page = parseInt($("#page").val())
	$("#page").val(page + 1)
	$("#users").append(users_view)
}
else{
	$("#page").parent().hide()
}